# Project Status

> Keep this file up to date. Update it in the same turn as any implementation work.

## Current Focus

MVP push — see [docs/roadmap.md](roadmap.md). M3 (jobs UI, #21/#22/#26) and M4 (security hardening, #27/#28) are both implemented and verified E2E together on 2026-07-27 on `feat/m4-security-hardening` (branched from `feat/jobs-ui`), since M4 changes the exact transport (agent `Connect`) and auth endpoints M3's demoed loop depends on. Combined pass against a real local hub/Postgres/agent, no mocks:

- **M3 regression (plaintext, `agent pair --insecure`):** dispatch → REST 201 → live status accepted→running→succeeded, exit_code=0 persisted; long job dispatch → cancel → accepted→running→stopping→cancelled; logs correct. Confirmed via REST polling and again through the browser (dispatch form → job detail live status/log/exit-code → jobs list badges).
- **M4 TLS:** hub fatals on partial `GRPC_TLS_CERT_FILE`/`GRPC_TLS_KEY_FILE` (both directions); with both set, serves real TLS 1.3 (`openssl s_client` confirmed); agent's default (no `--insecure`) correctly refuses a plaintext hub and an untrusted self-signed cert; full happy path verified by trusting a SAN-cert via `SSL_CERT_FILE` (process-scoped, no system trust store changes) — dispatch/exit-code/logs all work over the real TLS connection.
- **M4 tokens:** `GET /api/v1/agent-tokens` lists with no `token_hash` leak; revoking from a *different* org's JWT 404s (verifies the `AgentTokenRepo.Revoke` org-scoping fix); same-org revoke 204s and the token drops from the list; double-revoke 404s; an already-open gRPC stream survives its token being revoked (per the documented trade-off) and keeps accepting dispatch; a fresh `Connect` with the revoked token is rejected (`Unauthenticated: invalid agent token`). `/tokens` UI verified in-browser: list, revoke button, confirm dialog, row disappears on revoke.
- **M4 rate limiting:** hammering `POST /api/v1/device/authorize` allows exactly the burst (10) then returns `429` with `Retry-After: 10`.
- Side finding (not a bug, confirms ADR 0007): booting a second hub instance against the same dev DB for the cert-validation checks flipped the M3 test agent to `offline` via its startup `MarkAllOffline` — live demonstration of the single-instance constraint.

Since then, front ↔ hub gained a machine-readable contract (ADR 0010, 2026-08-08): `libs/harmost-api/openapi.yaml` now generates the Go DTOs and the front's TS types, closing the last boundary that was kept in sync by hand. `harmost-api:check` is ready to drop into M5's CI alongside the planned buf checks.

Next: M5 (deployment & packaging). Decided 2026-07-27: hub deploys self-hosted on Pablo's own server behind Cloudflare Tunnel (already used to manage the server), not Fly.io/Railway — Postgres is an existing external instance the hub just connects to; `cloudflared` needs HTTP/2 ingress configured for the gRPC endpoint (long-lived bidi stream, not plain HTTP).

## Recent Changes

| Date | App | Summary |
|------|-----|---------|
| 2026-08-17 | proto, api, hub, agent, front, docs | Unpair/delete an agent — new `DELETE /api/v1/agents/{id}` (`deleteAgent` in `openapi.yaml`). Soft delete: `agents.deleted_at` (migration 010) keeps the row (and its `jobs.agent_id` history — that FK has no cascade) while `GetByID`/`ListByOrg` filter it out; the handler also revokes every live `agent_tokens` row for the agent (`AgentTokenRepo.RevokeByAgentID`). `grpcapi.Server.Kick` (new — first proactive stream-kick in the codebase, via a per-agent kill channel on the stream registry) sends a new `HubMessage.unpair` proto message down the agent's open stream, if any, before force-closing it, rather than just dropping the connection and leaving the agent to guess why. Agent: on `HubMessage_Unpair` it clears its persisted `config.json` (new `config.Clear`) and returns a distinct `grpc.ErrUnpaired` from `Client.Connect`; the daemon reconnect loop (`internal/daemon/program.go`) recognizes it and stops retrying instead of looping every 60s against a revoked token — the process/service stays up idle, `agent pair` re-pairs it. Front: destructive "Unpair agent" button + shadcn `AlertDialog` confirmation on `/agents/$id` only (not the dashboard card), redirects to `/dashboard` on success |
| 2026-08-17 | front, docs | Sidebar componentized (`SidebarHeader`/`SidebarNav`/`SidebarFooter`, was one file) — `SidebarFooter` now shows the signed-in user via a shadcn `Avatar` + `DropdownMenu` (name, sign out) instead of an inline block, calling `useMe()` directly rather than taking `user`/`onLogout` as props; nav gained a `Projects` entry. `SidebarHeader` and `TopBar` both fixed to `h-14` so their borders line up. Dashboard/jobs-list action buttons paired with an inline description so they don't float alone in an otherwise-empty row. Dropped a stray `apps/front/package-lock.json` (the npm-based Nx workspace uses the root lockfile only). `apps/front/CLAUDE.md`'s boundary rules updated to note `SidebarFooter` as a second (besides `routes/_authenticated.tsx`) narrow exception to `shared/` never importing `features/` |
| 2026-08-17 | front, docs | Persistent top bar for the authenticated shell — new `TopBar` (route-driven breadcrumbs, e.g. `Jobs / New job`), rendered by `AppLayout` above every page. `PageContainer` dropped its `title`/`description`/`actions` header entirely (now just a padding/max-width wrapper) to avoid a duplicate title; each of its 7 callers moved that content into the page body instead — `agents/$id` and `jobs/$id` also lost their now-redundant manual `← back / id` breadcrumb rows in favor of an in-body header block (real entity name/status, since the top bar's crumb only shows a generic truncated id) |
| 2026-08-17 | front, docs | Authenticated shell restructure (ADR 0011, supersedes ADR 0009's `AppShell`-in-`app/` rule) — `AppShell` (top header) replaced by a sidebar layout; every protected route (`dashboard`, `agents/$id`, `jobs/*`, `tokens`) moved under a new `routes/_authenticated/` pathless layout route that owns the single `beforeLoad` auth guard (previously copy-pasted per route file) and composes the shell (`useMe()` + `AppLayout` + `SidebarContainer`). Presentational pieces (`AppLayout`, `SidebarContainer`, `PageContainer`, `AuthLayout`) now live in `shared/components/layout/`, all taking data as props — no feature-hook calls outside `routes/_authenticated.tsx`. `apps/front/CLAUDE.md` and ADR 0009 updated to match |
| 2026-08-12 | proto, agent, hub, front | Containers view phase 2 (#44) — stopped/exited containers, lifecycle actions, per-container resource usage/ports/volumes, on top of #43's watch pipeline. `ContainerListUpdate` now carries every container on the host (any state — filtering is a front-end concern), each with `ports`/`volumes` and, for running ones, a live `stats` sample (`docker.ContainerStats` via `ContainerStatsOneShot`-equivalent with `IncludePreviousSample`, ~1s per running container per tick — the standard CPU-delta formula needs two samples). New `HubMessage.container_action`/`AgentMessage.container_action_result` let the hub ask the agent to start/stop/restart/remove **any** container on the host (not scoped to harmost-dispatched jobs — deliberate, per user decision on #44's open question), run off the recv goroutine since Stop/Restart can take several seconds, reported both as a one-off `agent.container_action` WS event and via an immediate out-of-band `agent.containers` push so the UI doesn't wait out the ~5s tick. `RemoveContainer` never force-removes — a running container is refused with Docker's own error, and the front disables Remove for running/paused/restarting rows accordingly. Four new REST endpoints (`POST /api/v1/agents/{id}/containers/{containerId}/{start,stop,restart,remove}`), 202/404/409 mirroring `dispatchJob`. Verified end-to-end against a real local hub/Postgres/agent/Docker: stopped containers now visible (a pre-existing exited container on the test host showed up for the first time), ports/volumes/stats correctly populated for running containers, stop→exited settles within ~1s (occasionally races one stale `running` push against the fresh one from the same instant — coalescing-channel arrival-order artifact, self-corrects on the next regular tick, not a correctness bug), restart/start/stop all confirmed, remove refused on a running container with Docker's exact error surfaced through the WS event, remove succeeds once stopped |
| 2026-08-11 | proto, agent, hub, front | Agent details: live running-containers view (#43) — Docker-Desktop-style read-only, running-only container list on `/agents/$id`. New `HubMessage.watch_containers`/`unwatch_containers` and `AgentMessage.container_list` in `agent.proto`; the agent pushes `ContainerListUpdate` (from `docker.ListRunningContainers`, running-state filtered) every ~5s while watched, coalescing so only the latest list is ever queued. Hub refcounts watchers per agent (`grpcapi.registry.watchers`) so multiple viewers share one upstream push, forwards updates as a new `agent.containers` WS event (`ContainersPayload` in `openapi.yaml`, alongside two new REST commands `POST /api/v1/agents/{id}/containers/{watch,unwatch}` — per ADR 0002/0010, the subscribe/unsubscribe *command* goes over REST, only the push is WS), and resends `WatchContainersRequest` on agent reconnect if still watched. Front's `useAgentContainers` calls watch on mount / unwatch on unmount, with a `pagehide` + `fetch(..., {keepalive:true})` fallback for tab-close (`sendBeacon` can't carry the bearer token). Verified end-to-end against a real local hub/Postgres/agent/Docker: REST 202/404 org-scoping, WS frames arriving every ~5s with real running containers (including a live-started test container), agent flips back to `offline` on disconnect |
| 2026-08-08 | api, hub, front | **OpenAPI contract for front ↔ hub** (ADR 0010) — new `libs/harmost-api`, mirroring `libs/harmost-proto`'s role for hub ↔ agent. `openapi.yaml` describes all 14 JSON operations plus the two `/auth/github` redirects and `/ws`, and carries the WS event payloads as `components/schemas` (OpenAPI has no channel concept, so `HubEvent` is a `oneOf` with no `paths` entry). `nx run harmost-api:generate` emits Go models (`oapi-codegen`, models-only — handlers keep their shape) and TS types (`openapi-typescript` → `shared/api/schema.d.ts`); both committed, as `proto/gen` already is. Every hand-written DTO in `httpapi/` and each feature's `api/types.ts` now come from the one document. Because `wsClient.ts` takes its event types from a generated file rather than a feature, it can finally name `JobState` without breaking ADR 0008's `shared/ → features/` rule — both `as JobState` casts deleted. Generating the enum from the hub's real states surfaced a missing `unspecified` member the front never handled (`JobStateBadge` now renders it). `oapi-codegen` installs via a go.mod `tool` directive — no fourth undocumented GOPATH binary. Added `harmost-api:check` (regenerate + `git diff --exit-code`) and `front:typecheck`, which ADR 0001 and `apps/front/CLAUDE.md` had documented for months but was never actually defined. Since the migration had to be invisible on the wire, it is pinned by new marshalling tests in `httpapi` and `events` — `exit_code: 0` still serialized, unset `JobSpec` fields still omitted, `JobSpec` round-tripping losslessly through `convert.go`, no `token_hash`, and `Event.OrgID` still never reaching a client |
| 2026-08-03 | front | Metrics card polish — each metric is now a bordered stat tile (dot + label, `text-2xl` headline, capacity sublabel on a reserved line so the three plots share a baseline, plot). Dropped the per-chart y-axis *labels*: three identical 0/50/100 gutters were the card's loudest element and each one pushed its plot out of line with its own headline. Nothing is drawn inside the plot at all — the fixed 0–100 domain is carried by a recessed bed (`bg-background/60`) that bounds it, plus `10m … now` span labels derived from `windowMs`. That bed is there for the flat case: an idle agent's disk line never moves, and a bare sparkline of it read as a stray rule rather than a chart. Card chrome moved onto the shadcn slots it was fighting (`CardAction` for the "Updated …" trigger instead of overriding `CardHeader`'s grid, `CardDescription` naming the window). Footer is always rendered — "0 running containers" instead of the row vanishing — and hosts the "Collecting samples…" hint, so neither can resize the card; `Card` carries `pb-4` so its own `py-6` stops leaving the footer line high in its band. Chart tooltip folded to one line (time · value, no label row or swatch, `min-w-0` over `ChartTooltipContent`'s `min-w-[8rem]`) — the default stack covered most of a 205×56 plot |
| 2026-08-01 | front | Agent metrics card — the three radial gauges are now three side-by-side shadcn/recharts area charts (CPU/Memory/Disk over time), each with the current value as its headline, a fixed 0–100% y-scale so the facets stay comparable, and a crosshair tooltip. The hub persists only the latest snapshot, so history is buffered client-side by `useMetricsHistory`: a per-mount rolling 10-minute window (`METRICS_WINDOW_MS`, ≈20 points at the 30s heartbeat), pruned on append rather than on a timer. Charts start empty and fill in from the right — the x-axis spans the whole window, not the data extent. `RadialGauge` is no longer rendered by the app; it survives only as a design-system entry (barrel export + design-sync preview) |
| 2026-08-01 | repo | Removed Adminer from `compose.dev.yaml` — DataGrip/psql cover DB browsing, dropped the redundant browser client and its docs mentions (dev.md, README) |
| 2026-07-27 | hub | gRPC TLS (M4, #27) — `GRPC_TLS_CERT_FILE`/`GRPC_TLS_KEY_FILE` env; serves TLS when both set, refuses to start if only one is, warns on plaintext when `ENV=production` |
| 2026-07-27 | agent | gRPC TLS client (M4, #27) — `Connect` dials with the system cert pool by default; `agent pair --insecure` persists `insecure: true` in config for local dev (plaintext) |
| 2026-07-27 | hub | Agent-token management (M4, #28) — `GET /api/v1/agent-tokens` + `POST /api/v1/agent-tokens/{id}/revoke`, org-scoped (fixed `AgentTokenRepo.Revoke` to filter by `org_id`, previously any org could revoke any token by ID); revocation takes effect on next validation, does not drop an already-open gRPC stream |
| 2026-07-27 | front | `/tokens` — agent token list with revoke action (M4, #28) |
| 2026-07-27 | hub | Per-IP rate limiting (M4, #28) — hand-rolled token bucket (no new dep) on `/auth/github*` and `/api/v1/device/*`, 429 + `Retry-After` on limit |
| 2026-07-19 | proto, agent, hub | Exit code presence fix (found in M3 E2E) — `JobStatusUpdate.exit_code` was plain `int32`, and the hub dropped zeros (`if u.ExitCode != 0`), so successful jobs persisted `NULL`; now `optional int32`, the agent sends a pointer only when the container produced an exit code, and the hub forwards presence as-is |
| 2026-07-19 | hub | `GET /api/v1/me` (user profile from JWT claims) and `POST /api/v1/jobs/{id}/cancel` (org-scoped lookup, 409 on terminal state or disconnected agent, forwards `CancelJobRequest` over the gRPC stream; agent drives the state transition) (M3, #26/#21) |
| 2026-07-19 | front | Jobs UI (M3, #21/#22) — `/jobs` list with live state badges, `/jobs/new` dispatch form (online agents, image/command/args/env/timeout), `/jobs/$id` detail with spec/timing/exit code, cancel button, and live log viewer (REST backfill + WS `job.log` append, sequence-deduped, stderr styling, auto-scroll with pause-on-scroll-up) |
| 2026-07-19 | front | Session hardening (M3, #26) — `useHubEvents` WS hook with exponential-backoff auto-reconnect (replaces `useAgentEvents`), 401 responses clear the token and redirect to login, shared `AppShell` header with nav + `/me` user identity |
| 2026-07-18 | hub | Bounded gRPC shutdown — `GracefulStop` never returns while agent bidi streams are open, so a restarting hub held :8080 and the new process failed to bind; graceful phase now capped at 5s then hard `Stop` (found during M2 E2E) |
| 2026-07-18 | hub | `cmd/devtoken` — prints a signed JWT for headless API testing (`go run ./cmd/devtoken <user-id> <org-id>`, reads `JWT_SECRET` from env) |
| 2026-07-18 | hub | Job events over WS (M2, #25) — `job.status` published from JobService only when a guarded update applies; `job.log` one event per job per flush batch; terminal-state guard moved into SQL (`state NOT IN terminal`, RowsAffected = applied) with `domain.IsTerminal` as the single source |
| 2026-07-18 | hub | Lifecycle resilience (M2, #10) — dispatch rejects unknown/foreign agents (404, org-scoped lookup) and disconnected agents (409, no job row); `ReconcileAgent` 15s after hello fails jobs absent from `running_job_ids` (jobs created after the hello exempt); 30s orphan sweeper fails jobs on agents offline >2m; startup `MarkAllOffline` recovers crash leftovers |
| 2026-07-18 | agent | Process-lifetime send queue (M2) — statusCh/logCh moved from per-Connect closure onto Client so job goroutines survive reconnects; hello reports `running_job_ids`; failed StatusUpdate sends stashed and resent on reconnect; terminal statuses evict oldest on overflow instead of dropping |
| 2026-07-18 | proto | `AgentHello.running_job_ids = 5` (backward compatible) for hub-side reconciliation |
| 2026-07-18 | hub, agent | First unit tests (testify) — JobService status/reconcile/sweep, event bus scoping/overflow, agent Send routing + eviction |
| 2026-07-13 | docs | Docs tidy-up — root README written (was empty); root CLAUDE.md overview/monorepo sections filled; architecture.md corrected (job dispatch is REST not WS, webhooks marked planned, JWT auth not DB sessions, GORM not pgx, libs/harmost-proto documented, ADRs 0004–0006 linked); roadmap M1 marked done and stale Docker blocker cleared; app READMEs/CLAUDE.md files synced (agent `containers` + `internal/docker`, hub webhook mention removed, front routes + real Nx targets) |
| 2026-07-13 | agent | Docker executor (M1) — run.go full container lifecycle (pull per policy → create → start → log streaming with stdout/stderr demux → wait, force-remove cleanup); manager.go Dispatch/Cancel with per-job timeout ctx and terminal JobState mapping |
| 2026-07-13 | agent | gRPC job wiring — DispatchJob/CancelJob handled, job status/log messages funneled through send channel (single-sender stream), Ping now answered with Pong; daemon creates Docker+Manager at boot (degrades gracefully without Docker) |
| 2026-07-13 | agent | `agent containers` debug command — lists all host containers with their harmost job ID; runnable via `nx run agent:containers` |
| 2026-07-13 | agent | internal/docker started (M1) — moby SDK wrapper (New/Ping/ListAllContainers) + JobSpec→container.Config translation in spec.go |
| 2026-07-08 | docs | docs/roadmap.md — 8-week MVP plan; GitHub tracker reconciled (12 stale issues closed, milestones M1–M6, issues #25–#33) |
| 2026-07-08 | repo | Dev toolset — compose.dev.yaml (Postgres+Adminer), workspace Nx targets (db, db:reset, dev, grpc:ui), apps/hub/api.http, docs/dev.md, root .envrc; installed grpcurl/grpcui/dlv |
| 2026-06-19 | hub | Agent metrics — migration 009 adds metrics columns to agents; heartbeats persist CPU/mem/disk snapshots, exposed via REST |
| 2026-06-19 | hub | Migration 008 — agent_tokens.agent_id binds pairing tokens to their agent |
| 2026-06-19 | agent | Host metrics via gopsutil — CPU, memory, disk collected and sent in heartbeats |
| 2026-06-19 | front | /agents/$id — agent detail page with live metric gauges (WebSocket) |
| 2026-06-13 | hub | Migration 007 — alter users (github_id, name, avatar_url), add agent_tokens + device_codes tables |
| 2026-06-13 | hub | GitHub OAuth — /auth/github + /auth/github/callback; issues signed JWT; redirects to frontend |
| 2026-06-13 | hub | JWT auth middleware replaces x-org-id placeholder on all protected HTTP routes |
| 2026-06-13 | hub | Agent token auth — gRPC Connect validates `authorization: Bearer <token>` against agent_tokens table |
| 2026-06-13 | hub | Device flow — POST /api/v1/device/authorize, POST /api/v1/device/token, POST /api/v1/device/approve |
| 2026-06-13 | hub | WebSocket /ws?token=<jwt> — streams agent.connected/disconnected/heartbeat events to frontend clients |
| 2026-06-13 | hub | internal/auth/jwt.go + internal/events/bus.go — JWT helpers and pub/sub event bus |
| 2026-06-13 | agent | pair command — device flow: POST authorize, print URL, poll token endpoint, persist config |
| 2026-06-13 | agent | daemon — gRPC connect loop with Bearer token auth, AgentHello, 30s heartbeats, exponential backoff |
| 2026-06-13 | agent | internal/config — OS-appropriate config.json load/save |
| 2026-06-13 | front | Vite proxy → hub:8080 for /auth, /api, /ws |
| 2026-06-13 | front | /login — GitHub OAuth button; /auth/callback — stores JWT, redirects |
| 2026-06-13 | front | /dashboard — agent list (REST) + live status updates (WebSocket) |
| 2026-06-13 | front | /device?code=XXXX — device approval page |
| 2026-06-12 | hub | main.go wired — gRPC (:50051) + HTTP (:8080) servers, graceful shutdown |
| 2026-06-12 | hub | HTTP transport — chi router, REST endpoints for agents + jobs |
| 2026-06-12 | hub | gRPC transport — AgentService.Connect bidirectional stream; in-memory stream registry |
| 2026-06-12 | hub | Service + repository layers — all domain entities backed by GORM |
| 2026-06-12 | hub | Domain models + 6 goose migrations — users, orgs, org_members, agents, jobs, job_logs |
| 2026-06-12 | proto | gRPC contract — AgentService.Connect bidi stream; shared go.work module |
| 2026-06-11 | hub | Scaffolded hub structure — layered arch, tooling, ADR 0005 |
| 2026-06-11 | docs | Multi-tenancy model — ADR 0004 |
| 2026-06-06 | docs | Architecture decisions — ADR 0002, ADR 0003 |
| 2026-06-06 | front | Tailwind v4, shadcn/ui, TanStack Router + Query |
| 2026-06-06 | repo | Initial Nx monorepo scaffolding |

## Pending / Backlog

- [ ] Standalone `POST /api/v1/agent-tokens/{id}/revoke` still doesn't drop an already-open gRPC stream — `Validate` is only checked at `Connect` time (accepted M4 trade-off). `grpcapi.Server.Kick` now exists and is used by `DELETE /api/v1/agents/{id}` (unpair), so wiring `revokeAgentToken` to call it too would close this gap in the remaining path
- [ ] Server-side per-job WS event filtering — hub fans out all org events to every client; clients filter on `job_id` (fine at MVP scale)
- [ ] Org-scope `GET /jobs/{id}` and `GET /jobs/{id}/logs` — currently any authenticated user can read any job by ID (cancel is org-scoped; candidate for M4 hardening)
- [ ] Cancel-on-reconcile-mismatch — a job failed by the sweeper during a long partition keeps running on the agent; its late terminal status is dropped by the guard and no CancelJob is sent (accepted M2 trade-off)
- [ ] JobSpec HostConfig mapping — volume mounts, resource limits, network mode, privileged not yet translated (post-MVP per spec.go)
- [ ] Multi-org support — org switcher; users currently always use their personal org
- [ ] Container-watch refcount has no hard expiry — the `pagehide`/unmount unwatch is best-effort; a hard-killed tab/browser leaks a watcher (agent keeps polling Docker every ~5s) until the underlying gRPC stream drops for some other reason (#43 phase 1 trade-off)

## Known Issues / Blockers

_None currently. (Docker Desktop WSL integration was enabled on 2026-07-13, unblocking local Postgres and the agent's Docker executor.)_
